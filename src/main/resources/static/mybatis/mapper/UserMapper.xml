<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.velog.mapper.UserMapper">
    <insert id="signUp">
        insert into user(userEmail, userPw, userName, userAddr, userImg) values (#{userEmail},#{userPw}, #{userName}, #{userAddr}, #{userImg})
    </insert>
</mapper>