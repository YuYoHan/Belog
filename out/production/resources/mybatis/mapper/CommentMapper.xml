<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.Belog.mapper.CommentMapper">

    <insert id="add" useGeneratedKeys="true" keyProperty="commentNum">
        INSERT INTO comment(userId, boardNum, comment)
        VALUES (#{userId}, #{boardNum}, #{comment})
    </insert>

    <update id="edit" parameterType="com.example.Belog.domain.CommentDTO">
        UPDATE comment
        SET comment = #{comment}, commentTime=sysdate
        where commentNum = #{commentNum}
    </update>

    <delete id="delete" parameterType="com.example.Belog.domain.CommentDTO">
        delete from comment
        where commentNum = #{commentNum}
    </delete>

    <select id="findAll" resultType="com.example.Belog.domain.CommentDTO">
        SELECT c.boardNum, c.commentNum, u.userEmail, c.comment, c.commentTime
        FROM comment c, user u
        where c.userId = u.userId and c.boardNum = #{boardNum}
    </select>

    <select id="count" resultType="int">
        select count(*) from comment
        where boardNum = #{boardNum}
    </select>
</mapper>